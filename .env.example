# Get this from your Neon project dashboard -> Connection Details
# Use the "pooled" connection string for the app; drizzle-kit migrations
# can use either.
DATABASE_URL=postgresql://user:password@ep-xxxx.neon.tech/me2?sslmode=require

PORT=4000

# Signs login tokens. Generate one with: openssl rand -hex 32
JWT_SECRET=change-me
